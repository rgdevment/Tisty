name: CLA

# Dual licensing only works if every merged line can be relicensed, so a
# contribution cannot land before its author has signed. Signatures are stored
# in a separate repository rather than this one to keep the audit trail intact
# even if this repo's history is ever rewritten.
#
# Required in main's branch protection, or it reports and the merge happens
# anyway. It only sees Pull Requests, so CONTRIBUTING.md rules out the routes
# that bypass it. Widening what CLA.md grants needs a new signatures path: the
# old signatures consented to the old text.
#
# Tools are deliberately NOT on the allowlist. A co-author that cannot sign
# blocks its own pull request, and that is the intended outcome: CONTRIBUTING
# asks for the credit line to name people only, so a blocked PR means the guide
# went unread. Reopening it without that trailer costs a minute.

on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  actions: write
  contents: read
  pull-requests: write
  statuses: write

jobs:
  cla:
    name: Check CLA signature
    runs-on: ubuntu-latest
    # Only react to the signature phrase or to PR events, never to ordinary
    # comment traffic.
    if: >
      (github.event_name == 'issue_comment' &&
       github.event.comment.body == 'I have read the CLA Document and I hereby sign the CLA') ||
      github.event_name == 'pull_request_target'
    steps:
      - name: CLA Assistant
        uses: contributor-assistant/github-action@v2.6.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Fine-grained PAT with contents:write on the signatures repository.
          # Without it the action cannot record a signature.
          PERSONAL_ACCESS_TOKEN: ${{ secrets.CLA_SIGNATURES_TOKEN }}
        with:
          # Scoped per project: one signatures repository serves every
          # dual-licensed repo, but a signature given for one CLA must never
          # count as consent for another project's.
          path-to-signatures: "signatures/tisty/v1/cla.json"
          path-to-document: "https://github.com/rgdevment/Tisty/blob/main/CLA.md"
          branch: "main"
          allowlist: rgdevment,dependabot[bot]
          remote-organization-name: rgdevment
          remote-repository-name: cla-signatures
          custom-notsigned-prcomment: >
            Thanks for the pull request. Tisty is released under the AGPL-3.0
            and offered under separate commercial terms, which requires a signature
            from every contributor before code can be merged. You keep the copyright
            on your work — see [CLA.md](https://github.com/rgdevment/Tisty/blob/main/CLA.md).
            To sign, post a comment on this pull request with exactly:
          custom-pr-sign-comment: "I have read the CLA Document and I hereby sign the CLA"
          custom-allsigned-prcomment: "All contributors have signed the CLA. Thanks!"
