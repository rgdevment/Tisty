name: CLA

on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  actions: write
  contents: read
  pull-requests: write
  statuses: write

jobs:
  cla:
    name: Check CLA signature
    runs-on: ubuntu-latest
    if: >
      (github.event_name == 'issue_comment' &&
       github.event.comment.body == 'I have read the CLA Document and I hereby sign the CLA') ||
      github.event_name == 'pull_request_target'
    steps:
      - name: CLA Assistant
        uses: contributor-assistant/github-action@v2.6.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PERSONAL_ACCESS_TOKEN: ${{ secrets.CLA_SIGNATURES_TOKEN }}
        with:
          path-to-signatures: "signatures/tisty/v1/cla.json"
          path-to-document: "https://github.com/rgdevment/Tisty/blob/main/CLA.md"
          branch: "main"
          allowlist: rgdevment,dependabot[bot]
          remote-organization-name: rgdevment
          remote-repository-name: cla-signatures
          custom-notsigned-prcomment: >
            Thanks for the pull request. Tisty is released under the AGPL-3.0
            and offered under separate commercial terms, which requires a signature
            from every contributor before code can be merged. You keep the copyright
            on your work — see [CLA.md](https://github.com/rgdevment/Tisty/blob/main/CLA.md).
            To sign, post a comment on this pull request with exactly:
          custom-pr-sign-comment: "I have read the CLA Document and I hereby sign the CLA"
          custom-allsigned-prcomment: "All contributors have signed the CLA. Thanks!"
