name: CI

on:
  push:
    branches: [main]
  pull_request:

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -D warnings

jobs:
  # Runs the tests, not just the build: path and locking bugs only show at runtime.
  test:
    name: test / ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # Linux is a phase of its own and nothing is shipped for it yet, so the
        # runners are the two platforms that are.
        os: [windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: ./.github/actions/sidecar
      - uses: taiki-e/install-action@nextest
      - run: cargo nextest run --workspace --no-tests=pass
      - run: cargo test --doc --workspace
      # Catches a suite that reads the locale off the runner instead of setting it.
      - run: cargo nextest run --workspace --no-tests=pass
        env:
          LC_ALL: es_ES.UTF-8
          LANG: es_ES.UTF-8

  lint:
    name: fmt + clippy
    # `--all-targets` reaches tisty-gui, which is only built where it ships.
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - uses: ./.github/actions/sidecar
      - run: cargo fmt --all --check
      - run: cargo clippy --workspace --all-targets

  window:
    name: frontend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: app
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: app/package-lock.json
      - run: npm ci
      - run: npm run build
      - run: npm test

  audit:
    name: advisories + licences
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: EmbarkStudios/cargo-deny-action@v2
