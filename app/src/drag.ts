/** A private type keeps the drop targets from claiming files, and vice versa. */
export const TASK = "application/x-tisty-task";

/** The inbox has no id, so the highlight needs a name of its own. */

/** Steps drag inside one task, so they never mix with tasks or files. */
export const STEP = "application/x-tisty-step";
